import { SafeAreaView, View } from "react-native";
import { Text } from "@/components/ui/text";

export default function Notifications() {
  return (
    <SafeAreaView>
      <Text className="text-zinc-200">Notifications</Text>
    </SafeAreaView>
  );
}
